import { Text } from '@chakra-ui/react';
import styled from '@emotion/styled';

// eslint-disable-next-line import/prefer-default-export
export const Text404 = styled(Text)`
  text-shadow: 8px 4px #1ab282;
  font-weight: bold;
`;
